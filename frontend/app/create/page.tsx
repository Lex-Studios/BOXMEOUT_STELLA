"use client";

/**
 * Create market page — client component (requires wallet).
 * Redirects to home if no wallet is connected.
 * Renders CreateMarketForm and handles post-submit navigation.
 */
export default function CreateMarketPage(): JSX.Element {
  throw new Error("Not implemented");
}
